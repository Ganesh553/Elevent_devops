pipeline {
    // add your slave label name
    agent { label 'Slave_Tomcat_Automation'}
    tools{
        maven '3.8.6'
    }
    stages {
        stage ('Checkout SCM') {

            steps {
          	    
	     checkout scm
            }
        }

        stage ('Build') {

            steps {
               sh 'mvn package'
            }
        }
        
        stage ('deploy') {

            steps {
               // update your tomcat server ip accordingly in below command
		    echo "deploying on tomcat server"
               sh "scp target/maven-web-application.war  ec2-user@:/3.26.217.173/tomcat9/webapps/"
                    
            }
        }

        
    }
}
